# Frontend (Vite / Netlify)
# Local dev: leave empty — Vite proxies /api → http://localhost:5000
# Production: set to your deployed backend URL (no trailing slash)
# Example: https://mogadishu-furniture-api.onrender.com
VITE_API_URL=
